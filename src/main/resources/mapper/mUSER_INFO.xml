<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User_Info">

   		
   		 <select id = "getUser_InfoSelect"  parameterType = "String"   resultType="User_InfoDto">
   		 	SELECT   USER_ID
					,USER_PW
					,USER_NAME
					,USER_HPNO
					,USER_EMAIL
					,USER_ADDR
					,USER_BIRTH
					,USER_GENDER
					,USER_UPDATED_AT
					,USER_UPDATED_AT
				FROM USER_INFO
				WHERE USER_ID = #{USER_ID}
   		 </select>
   		 
   		 
   		 <select id="getUser_InfoSelectAll" resultType="User_InfoDto">
   		 	SELECT 	 USER_ID
					,USER_PW
					,USER_NAME
					,USER_HPNO
					,USER_EMAIL
					,USER_ADDR
					,USER_BIRTH
					,USER_GENDER
					,USER_UPDATED_AT
					,USER_UPDATED_AT
				FROM USER_INFO
   		 </select>

		 <select id="existsById" parameterType="String" resultType="int">
		 	select count(user_id) from user_info
			<where>
			user_id = #{user_id}
			</where>
		 </select>

		 <insert id="insert" parameterType="User_InfoDto">
		 	insert into user_info
				(user_id, user_pw, user_name, user_hpno, user_email, 
				user_addr, user_birth, user_gender, user_created_at, user_updated_at)
			values
				(#{user_id}, #{user_pw}, #{user_name}, #{user_hpno}, #{user_email}, 
				#{user_addr}, #{user_birth}, #{user_gender}, #{user_created_at}, #{user_updated_at})
		 </insert>
        
</mapper>